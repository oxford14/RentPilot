rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow upload (write) but not read
      allow write: if true;
      allow read: if false;
    }
  }
}