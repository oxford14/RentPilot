rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access for client logos so they can be displayed.
    // Allow write access only for authenticated users.
    match /client_logos/{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
