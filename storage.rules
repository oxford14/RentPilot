rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read on all files, especially logos
    match /client_logos/{fileName} {
      allow read;
      // Allow writes only if the user is authenticated through Firebase.
      // This works with anonymous sign-in.
      allow write: if request.auth != null;
    }
  }
}
