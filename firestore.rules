rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to everyone for all collections.
    // WARNING: This is insecure for production and should be replaced
    // with authenticated rules before launch.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
