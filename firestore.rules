rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This rule allows read and write access to all collections for development.
    // WARNING: This is insecure for production. You must secure your rules later.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
