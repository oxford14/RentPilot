rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This is an insecure rule for development purposes only.
    // It allows read and write access to everyone for all collections.
    // You MUST secure this before going to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
